
# build 2026-04-04b
# ═══════════════════════════════════════════════════════
# Staylio.ai — Pipeline Requirements
# All 7 agents + LangGraph orchestration
# Updated: March 31, 2026
# ═══════════════════════════════════════════════════════

# ── Orchestration ────────────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.2.0

# ── AI APIs ──────────────────────────────────────────────
anthropic>=0.34.0
openai>=1.40.0

# ── Web Scraping (Agent 1) ───────────────────────────────
apify-client>=1.7.0
httpx>=0.27.0

# ── Database (all agents) ────────────────────────────────
supabase>=2.7.0

# ── Cache / Queue (all agents) ───────────────────────────
redis>=5.0.0
upstash-redis>=1.0.0

# ── Photo Enhancement (Agent 3) ──────────────────────────
Pillow>=10.0.0
requests>=2.31.0

# ── Google Cloud (Agent 3 + Agent 4) ────────────────────
google-cloud-vision>=3.7.0
google-auth>=2.29.0

# ── Video Generation (Agent 3) ───────────────────────────
runwayml>=4.10.0

# ── Analytics (Agent 7) ──────────────────────────────────
google-analytics-data>=0.18.0
segment-analytics-python>=2.3.0

# ── Cloudflare R2 / Pages (Agent 5) ─────────────────────
boto3>=1.34.0
cloudflare>=3.0.0

# ── Utilities ────────────────────────────────────────────
typing-extensions>=4.9.0
python-dotenv>=1.0.0
pydantic>=2.0.0
email-validator>=2.0.0
tenacity>=8.2.0
loguru>=0.7.0
aiohttp>=3.9.0


# ── Web server (Railway entry point) ─────────────────────
fastapi>=0.111.0
uvicorn>=0.30.0
